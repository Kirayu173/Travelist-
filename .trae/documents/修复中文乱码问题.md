## 问题分析
1. **乱码位置**：在 `trip_service.py` 文件第510行，错误信息显示为 `"??????"`，应为中文文本
2. **编码格式**：文件使用 ASCII 编码，导致中文内容无法正确显示
3. **其他文件**：检查了 `service.py` 和 `prompts.py` 等文件，中文显示正常

## 修复方案
1. **修复乱码内容**：将 `trip_service.py` 中的 `"??????"` 替换为正确的中文错误信息
2. **统一编码格式**：确保所有包含中文的文件使用 UTF-8 编码
3. **验证修复效果**：检查修复后的文件中文显示是否正常

## 实施步骤
1. 编辑 `trip_service.py` 文件，修复第510行的中文乱码
2. 检查其他包含中文的文件，确保编码格式正确
3. 验证所有中文内容显示正常

## 预期结果
所有中文内容显示正常，无乱码、错码或显示异常情况，文本信息准确传达。